@page
@model BuildingBuddies.Pages.Meetings.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Meeting[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Meeting[0].Link)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Meeting[0].Domain)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Meeting[0].CompanyName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Meeting[0].EndDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Meeting) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Link)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Domain)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MeetingID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MeetingID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MeetingID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
